# 平台题库（调试八十四期）识别方法设计

日期：2026-09-10
状态：已确认（方案 A）

## 背景

答题助手（pwa/）支持导入题库文件，[parser.js](../../../pwa/js/parser.js) 负责把 Word/PDF/Excel/JSON 解析成统一的 QA 对象（`{question, answer, type, options, source}`），存入 IndexedDB，由 search.js 用 Fuse.js 模糊搜索，Android 悬浮窗显示答案。

新文件 `调试八十四期题库.xlsx` 是答题平台的导出格式，现有 `parseExcel` 无法正确解析：

| 现有解析器假设 | 新文件实际情况 |
|---|---|
| 答案列存 A-D 字母 | 答案列存 `选项2`、`选项1/2/3`（多选）、`对`/`错` |
| 选项列表头是 `选项A` 等字母编号 | 选项列表头是 `选项1`~`选项6` 数字编号 |
| 答案即最终显示内容 | `选项2` 必须先解析成选项原文文字才有用 |

实测结果：现有解析器会把 68 道题的答案原样存成"选项2"这类无意义字符串，type 判为 unknown。

## 新文件格式（已实测分析）

表头：`题目类型 | 题目标签 | 答题时间（秒）| 问题内容 | 答案 | 选项随机排序 | 选项1~6 | 题目解读标题 | 题目解读内容`

- 共 68 行数据；题型分选择题（有选项）和是非题（无选项，答案 对/错）
- 答案列格式：`选项N`（单选）、`选项1/2/3`（多选，`/` 分隔，可能有尾随空格）、`对`/`错`
- **选项随机排序 = 是**：答题时每位用户的选项顺序随机打乱 → 序号/字母不可靠，答案必须存选项原文文字
- 单元格文字含 `\r\n` 换行（如"搭设高度24m 及以上的…（包括采光井、\r\n电梯井脚手架）"）和首尾空格，需清洗

## 用户确认的决策

1. 选择题答案存**纯选项原文文字**（如"立即停止作业并上报"），不存序号
2. 多选答案的多个选项文字用**换行 `\n` 分隔**
3. `题目解读标题`/`题目解读内容` 两列**忽略不存**

## 方案（A：独立函数 + 表头指纹自动分派）

### 修改文件

1. `pwa/js/parser.js` —— 主改动
2. `pwa/css/style.css` —— 小改动：`.match-a` 加 `white-space: pre-line`，让多选答案的换行在浏览器端正常显示
3. `tools/test_parser_platform.js` —— 新增 Node 测试脚本

不改：db.js、search.js、app.js、Android 壳（Kotlin）。

### parser.js 改动

**1. 新增工具函数**

- `cleanText(s)`：trim + `/\s+/g → ' '`（折叠换行为空格）
- `PLATFORM_OPTION_HEADER_RE = /^选项\s*([1-6])$/`（表头指纹）
- `ANSWER_REFS_RE = /^选项\s*([1-6])(?:\s*[/、，,]\s*([1-6]))*$/`（答案引用解析）

**2. `detectPlatformFormat(rows)`**

在前 5 行里找表头：同时存在"答案"列（现有 `/答案|answer|正确/` 正则）、问题列（`/问题内容/` 正则，匹配平台表头；不能用旧格式的 `/题目|问题|…/`，因为"题目类型"列会子串误命中）和 ≥1 个精确匹配 `选项N` 数字编号的列。
返回 `{headerRowIndex, qCol, aCol, optionCols}`；找不到返回 null。
旧格式用字母编号（选项A），与数字编号永不冲突，检测无歧义。

**3. `parsePlatformRows(rows, header, sourceName)`**

遍历数据行，逐行解析：

| 答案列内容（清洗后） | 处理 |
|---|---|
| 空题目行 | 跳过 |
| `对` / `错` | 走现有 makeQA → normalizeAnswer 转 `√`/`×`，type=truefalse |
| `选项N` / `选项1/2/3` | 提取序号数组 → 查选项列取原文文字（cleanText）→ 单选答案=文字；多选答案=文字按 `\n` 连接；type=choice |
| 引用的选项文字为空 | 兜底：答案保留原始"选项N"字符串，不丢数据 |
| 其他 | 走现有 makeQA 原样保留 |

- 解析出的**文字答案绕过 normalizeAnswer**（避免选项文字恰好含"对/错"被误转 √×）；对/错 判题仍走 normalizeAnswer
- options 数组存非空选项文字（按选项 1→6 原始顺序），供导出与后续扩展

**4. `parseExcel` 改造**

读完 rows 后先调 `detectPlatformFormat`，命中 → `parsePlatformRows` 返回；未命中 → 原逻辑一字不动。

### style.css 改动

`.match-a { white-space: pre-line; }` —— 浏览器 result-card 中 `\n` 默认折叠成空格，多选答案会挤成一行；pre-line 保留换行。Android 悬浮窗 TextView 原生支持 `\n`，无需改。

### 数据流（改后）

```
xlsx 文件 → parseExcel → 指纹命中新格式 → parsePlatformRows
  → QA: {question, answer: "立即停止作业并上报" / "严禁私自拆除\n确需拆除必须审批", type, options}
  → IndexedDB → search.js 搜索 → formatAnswer 原样返回文字答案 → 悬浮窗显示
```

search.js 的 formatAnswer 只处理 `^[A-H]$` 单字母答案，文字答案直接原样返回，无需改动。

## 测试

**Node 脚本 `tools/test_parser_platform.js`**：用 vm 沙箱加载 `pwa/libs/xlsx.full.min.js` 与 parser.js，File 用 stub 提供 arrayBuffer，实测解析 `调试八十四期题库.xlsx`：

- 共解析出 68 道题
- 无任何答案含"选项"字样（全部解析成文字或 √×）
- 选择题答案均为非空文字，type=choice
- 是非题答案均为 √/×，type=truefalse
- 多选答案含 `\n` 分隔
- 抽查 3 道题答案与题库原文逐字一致：
  1. "施工现场发现防护缺失、存在隐患，作业人员应当（）" → `立即停止作业并上报`（单选）
  2. "安全防护设施拆除原则（）" → `严禁私自拆除\n确需拆除必须审批\n施工结束立即恢复`（多选）
  3. "安全防护设施是保命设施，严禁随意拆除。" → `√`（是非题）

**回归测试**：用仓库已有旧格式题库文件（根目录 `2026-07-11 16_56_52拍照录题.xlsx`、`EXCEL_auto-help/`）跑现有 parseExcel 路径，结果与改动前一致（旧逻辑零改动，回归仅确认分派不误伤）。

**手工验证**：浏览器打开 pwa 导入新文件，预览显示"立即停止作业并上报"等文字答案。

## 验收标准

1. 导入 `调试八十四期题库.xlsx` → 识别 68 道题，选择题答案=选项原文，是非题=√/×，无"选项N"残留
2. 多选答案换行分隔，浏览器与悬浮窗均正常显示
3. 旧格式题库文件解析结果与改动前完全一致
